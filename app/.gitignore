# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/
.packages
pubspec.lock

# Generated l10n
lib/l10n/app_localizations*.dart

# Platform folders are generated locally via `flutter create .` (see README).
# Track them later if you want reproducible native config; for now they're local.
/android/
/ios/
/linux/
/macos/
/windows/
/web/

# Secrets — NEVER commit
lib/firebase_options.dart
android/app/google-services.json
ios/Runner/GoogleService-Info.plist

# Heavy / generated assets (built locally)
assets/quran/*.sqlite
*.onnx
